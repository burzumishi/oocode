# Generado desde el antiguo config.py — bloque 'workspace' de DEFAULT_CONFIG.
DEFAULTS = {
    "maxMemoryLines": 50,   # líneas de MEMORY.md en el mini-context
    "maxDailyChars":  2000  # chars del log diario en el mini-context
}
