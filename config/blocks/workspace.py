# Generado desde el antiguo config.py — bloque 'workspace' de DEFAULT_CONFIG.
DEFAULTS = {
    "maxMemoryLines": 50,    # líneas de MEMORY.md en el mini-context
    "maxDailyChars":  2000,  # chars del log diario en el mini-context
    "oocodeMdWarnKb": 8,     # aviso si OOCODE.md supera N KB (se carga entero igual)
    "oocodeMdMaxKb":  24,    # techo duro: trunca OOCODE.md con marcador si supera N KB
    "memoryFullMaxChars": 8000  # tope de chars de MEMORY.md en /ctx full (0 = sin tope)
}
