# Generado desde el antiguo config.py — bloque 'agents' de DEFAULT_CONFIG.
from config.constants import CONFIG_DIR

DEFAULTS = {
    "defaults": {
        "model": None,
        "workspace": str(CONFIG_DIR / "workspace" / "main")
    },
    "list": [
        {
            "id":        "main",
            "name":      "OOCode",
            "emoji":     "🤖",
            "model":     None,
            "workspace": str(CONFIG_DIR / "workspace" / "main")
        }
    ]
}
