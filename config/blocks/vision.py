# Generado desde el antiguo config.py — bloque 'vision' de DEFAULT_CONFIG.
DEFAULTS = {
    "enabled":       True,   # activar detección de imágenes en el input
    "showIndicator": True    # mostrar indicador de visión en la toolbar
}
